# Re-verified 2026-08-19 after pinning back to stagehand@3.6.0 (bugfix-001, to
# escape the 3.7.x shutdown-supervisor regression): both patched files/functions
# (aisdk.js inferProviderName, zodCompat.js toJsonSchema) still exist at the same
# dist/{cjs,esm} paths in 3.6.0 and the patched code is present after
# `pnpm install --frozen-lockfile` reapplies this patch cleanly. Keep both hunks.
diff --git a/dist/cjs/lib/v3/llm/aisdk.js b/dist/cjs/lib/v3/llm/aisdk.js
index 164294dfb71e05e45022b7d07e3bc5669a0ea265..fb1a660e3cb6b6621ec842c97455b02d17dc6528 100644
--- a/dist/cjs/lib/v3/llm/aisdk.js
+++ b/dist/cjs/lib/v3/llm/aisdk.js
@@ -9,6 +9,11 @@ const FlowLogger_js_1 = require("../flowlogger/FlowLogger.js");
 const zodCompat_js_1 = require("../zodCompat.js");
 function inferProviderName(modelId) {
     const [providerName] = modelId.split("/");
+    // AWS Bedrock model IDs (e.g. "us.anthropic.claude-opus-5") contain no
+    // "/", so providerName above is the whole ID, matching no switch case.
+    // Detect the Bedrock-embedded Anthropic vendor segment so
+    // structuredOutputMode still gets set for Bedrock-routed Claude models.
+    if (providerName && providerName.includes("anthropic.")) return "anthropic";
     return providerName || undefined;
 }
 class AISdkClient extends LLMClient_js_1.LLMClient {
diff --git a/dist/cjs/lib/v3/zodCompat.js b/dist/cjs/lib/v3/zodCompat.js
index eba13413fa397434d6c64f31e539cca7415f8a18..9fd232d43fbb8044a64b2c3c2dd52e1280d85ade 100644
--- a/dist/cjs/lib/v3/zodCompat.js
+++ b/dist/cjs/lib/v3/zodCompat.js
@@ -6,6 +6,7 @@ Object.defineProperty(exports, "__esModule", { value: true });
 exports.isZod3Schema = exports.isZod4Schema = void 0;
 exports.toJsonSchema = toJsonSchema;
 const zod_1 = require("zod");
+const zod_v4_1 = require("zod/v4");
 const zod_to_json_schema_1 = __importDefault(require("zod-to-json-schema"));
 const isZod4Schema = (schema) => typeof schema._zod !== "undefined";
 exports.isZod4Schema = isZod4Schema;
@@ -15,7 +16,12 @@ function toJsonSchema(schema) {
     if (!(0, exports.isZod4Schema)(schema)) {
         return (0, zod_to_json_schema_1.default)(schema);
     }
-    // For v4 schemas, use built-in z.toJSONSchema() method
+    // Use the v4 subpath of this module's own zod dependency, since the
+    // caller's schema may be a zod/v4 schema even when this module's
+    // default `z` import resolves to zod's v3 namespace.
+    if (zod_v4_1.z.toJSONSchema) {
+        return zod_v4_1.z.toJSONSchema(schema);
+    }
     const zodWithJsonSchema = zod_1.z;
     if (zodWithJsonSchema.toJSONSchema) {
         return zodWithJsonSchema.toJSONSchema(schema);
diff --git a/dist/esm/lib/v3/llm/aisdk.js b/dist/esm/lib/v3/llm/aisdk.js
index 76b32495d1dece84bbfd522260240ee8865bfae9..4df6382e1be0ae3c12bc3333280f858a6e451e6e 100644
--- a/dist/esm/lib/v3/llm/aisdk.js
+++ b/dist/esm/lib/v3/llm/aisdk.js
@@ -6,6 +6,11 @@ import { FlowLogger, extractLlmPromptSummary, } from "../flowlogger/FlowLogger.j
 import { toJsonSchema } from "../zodCompat.js";
 function inferProviderName(modelId) {
     const [providerName] = modelId.split("/");
+    // AWS Bedrock model IDs (e.g. "us.anthropic.claude-opus-5") contain no
+    // "/", so providerName above is the whole ID, matching no switch case.
+    // Detect the Bedrock-embedded Anthropic vendor segment so
+    // structuredOutputMode still gets set for Bedrock-routed Claude models.
+    if (providerName && providerName.includes("anthropic.")) return "anthropic";
     return providerName || undefined;
 }
 export class AISdkClient extends LLMClient {
diff --git a/dist/esm/lib/v3/zodCompat.js b/dist/esm/lib/v3/zodCompat.js
index e97ff17b9cb176b17ba37badd8b9b04354ec72ea..8744baa2d34d4d8aec7ed3cacde1fce5f0ec955c 100644
--- a/dist/esm/lib/v3/zodCompat.js
+++ b/dist/esm/lib/v3/zodCompat.js
@@ -1,4 +1,5 @@
 import { z } from "zod";
+import { z as z4 } from "zod/v4";
 import zodToJsonSchema from "zod-to-json-schema";
 export const isZod4Schema = (schema) => typeof schema._zod !== "undefined";
 export const isZod3Schema = (schema) => !isZod4Schema(schema);
@@ -6,7 +7,12 @@ export function toJsonSchema(schema) {
     if (!isZod4Schema(schema)) {
         return zodToJsonSchema(schema);
     }
-    // For v4 schemas, use built-in z.toJSONSchema() method
+    // Use the v4 subpath of this module's own zod dependency, since the
+    // caller's schema may be a zod/v4 schema even when this module's
+    // default `z` import resolves to zod's v3 namespace.
+    if (z4.toJSONSchema) {
+        return z4.toJSONSchema(schema);
+    }
     const zodWithJsonSchema = z;
     if (zodWithJsonSchema.toJSONSchema) {
         return zodWithJsonSchema.toJSONSchema(schema);
