MANUAL VERIFICATION (Playwright, live (unnamed) ATS vendor, 2026-08-18)
=============================================================
Proves the `source` cascading multiselect commits under REAL trusted clicks,
while barnacle's automation does not.

The widget is a TWO-LEVEL cascading picker:
  - open multiselectInputContainer -> popup shows CATEGORIES
    (Advertising, General, Job Boards, Job Fair, Military, Networking)
  - click a category -> drill GET /values/sources/sources/<catId> -> popup shows LEAVES
    (under "Job Boards": Glassdoor, IndeedEasyApply, Internet - Job Boards/Search Engines, irishjobs)
  - click a LEAF -> POST /source commits the value, chip appears, aria-invalid clears

MANUAL RESULT (each step a real Playwright trusted click):
  - synthetic el.click() on category "Job Boards": did NOT expand (still showed categories)
  - TRUSTED Playwright click on category "Job Boards": expanded to leaves  [drill GET #147 fired]
  - TRUSTED Playwright click on leaf "Internet - Job Boards/Search Engines":
        -> committed: selectedItem chip = "Internet - Job Boards/Search Engines"
        -> aria-invalid cleared (stillInvalid=false)
        -> POST /source #148 [200] fired (value persisted server-side)

BARNACLE RESULT (recon runs 6 & 7, barnacle 1.12.3):
  - step 10 (category): "resolved by prompt-selector primitive" / or view-swap — category
    clicked but in run 7 the drill often didn't expand
  - step 11 (leaf): "focused probe found 0 candidates ... succeeded via act-string + el.click() fallback"
    -> the leaf click went through bare el.click() (the primitive did NOT run on the leaf)
    -> POST /source body stayed {} across the whole run
    -> source aria-invalid at final Submit -> Submit bounced to "step 1 of 7"
  Reproduced across runs 6 and 7 (3 total incl. run 5); source never committed once under barnacle.
