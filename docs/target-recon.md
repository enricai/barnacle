<!-- Run `pnpm run recon:summarize` to populate this file after a recon run. -->
<!-- Until then, see src/sites/example/contract.ts for the codified contract pattern. -->

# Target Recon Findings

> This file is generated by `pnpm run recon:summarize` after running the recon
> pipeline against a target site. Commit it alongside the plugin — it is the
> evidence log that justifies every constant in the contract file and the
> context a teammate needs when the integration breaks.

---

## What this looks like (filled-in example)

The section below shows what `recon:summarize` produces for a generic
e-commerce site. Your generated content replaces this example.

---

### Site

`https://example.com` — Generic e-commerce SPA

### User flow

`src/sites/example/recon-flow.json`:
`/catalog → category filter → /product/... detail page`

### Recon run

`2026-05-15T14:22:00Z` — 31 captures, 2 unique endpoints, ~88 seconds

---

### Endpoints found

| # | Method | URL | Operations |
|---|--------|-----|------------|
| 1 | POST | `https://example.com/graph` | `productSearch_Products`, `productSearch_Facets`, `product_Detail`, `cart_AddItem`, `user_Wishlist`, ... (28 ops) |
| 2 | GET | `https://example.com/catalog/static/locales/en.json` | — (fixture) |

---

### Public endpoints (no auth required)

All replays via plain Node `fetch()` with `RC_HEADERS` returned `200`:

- `https://example.com/graph` — all operations listed above

---

### Auth-required endpoints

None found. All tested endpoints are public.

---

### Rate-limit ceiling

Probe: 60 sequential requests at 1 → 3 → 5 rps. Zero `429`/`403` responses
at any rps level.

**Safe ceiling: 5 rps.** Ready-to-paste Bottleneck config:

```ts
const limiter = new Bottleneck({ minTime: 200 }); // 5 rps
```

---

### Load-bearing headers

Headers identified as required for successful replay (start minimal — only
these are committed to `BASE_HEADERS`):

| Header | Value committed | Why load-bearing |
|--------|----------------|-----------------|
| `Content-Type` | `application/json` | Required for POST body parsing |
| `Accept` | `application/json, */*` | Server returns 406 without it |
| `Origin` | `https://example.com` | Empty-body response without it |
| `Referer` | `https://example.com/` | Empty-body response without it |
| `User-Agent` | `Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7) ...` | 403 from edge without it |

All other headers captured during recon are decorative and not committed.

---

### Hazards

| Hazard | Status | Notes |
|--------|--------|-------|
| Akamai edge | Not detected | No `AkamaiGHost` response headers |
| Cloudflare | Not detected | No `cf-ray` response headers |
| Bot manager | Not detected | All replays 200 with minimal headers |
| CAPTCHA | Not detected | `SCRAPER_SOLVE_CAPTCHA` not needed for hot path |

---

### Auxiliary fixtures

Static JSON endpoints flagged for commit:

| File | URL | Update frequency |
|------|-----|-----------------|
| `src/sites/example/fixtures/locales-en.json` | `https://example.com/catalog/static/locales/en.json` | Rarely (deploy-gated) |

Load at startup via `src/scraper/fixtures.ts` rather than fetching per-request.

---

### GraphQL introspection

Disabled on `https://example.com/graph` — responded `200` with
`{"errors":[{"message":"introspection is disabled"}]}`. Zod schemas written by
hand against captured JSON.

---

### Filter encoding

`productSearch_Products` accepts a `filters` variable containing a
proprietary key-value string. The recon capture helper decoded it automatically
(base64 → JSON). Committed as a helper in `src/sites/example/contract.ts`.

---

### Committed contract

- **Query:** `productSearch_Products` (lean hand-trimmed subset) → `src/sites/example/contract.ts`
- **Headers:** `BASE_HEADERS` constant in `src/sites/example/contract.ts`
- **Limiter:** `new Bottleneck({ minTime: 200 })` in `src/sites/example/contract.ts`
- **Zod schemas:** `ExampleResponseSchema`, `ExamplePayloadSchema` in `src/sites/example/contract.ts`

---

### Maintenance

When the smoke test fails:

1. `pnpm run recon:browser -- --url https://example.com --flow-file src/sites/example/recon-flow.json`
2. Diff `/tmp/recon/graphql/*productSearch_Products*.json` against the committed query in `contract.ts`
3. Update query / headers / Zod schema / throttle config as needed
4. `pnpm run recon:summarize` (updates this file)
5. Ship PR

---

<!-- ↑ Example ends here. Your generated content from recon:summarize replaces the above. ↑ -->
